// src/environments/environment.ts
export const environment = {
  production: false,
  enableAuth: false,          // feature flag: sin login
  apiBaseUrl: '/api',         // usa proxy o variable mínima
};
